# AlmanOnline-backend

clone

`https://github.com/RezaHajipour/AlmanOnline`

cd into dir

`cd AlmanOnline`

install dependencies

`npm i`

add credentials

`.env`

run with nodemon

`npm run dev:server;`

`http://localhost:3001/`
